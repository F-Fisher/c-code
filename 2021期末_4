#include<stdio.h>
#include<stdlib.h>
 void swap(int* a, int* b)
{
	int c;
	c = *a;
	*a = *b;
	*b = c;
	return;

}
int main()
{
	int a, b;
	printf("in\n");
	scanf("%d", &a);
	scanf("%d", &b);
	printf("a=%d\nb=%d\n", a, b);
	swap(&a, &b);
	printf("a=%d\nb=%d", a, b);
	return 0;
}
